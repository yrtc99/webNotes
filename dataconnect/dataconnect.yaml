specVersion: "v1beta"
serviceId: "project1"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "project1-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
